# مراحل بعدی پس از ایجاد دیتابیس در Liara

## ✅ مرحله 1: بررسی جداول (تأیید شده)

اگر جداول را ایجاد کرده‌اید، در phpMyAdmin باید سه جدول ببینید:
- ✅ `companies`
- ✅ `document_covers`
- ✅ `document_files`

## 🔧 مرحله 2: بررسی فایل .env در Backend

مطمئن شوید که فایل `backend/.env` با اطلاعات زیر وجود دارد:

```env
PORT=5000
DB_HOST=hesabdoni
DB_PORT=3306
DB_USER=root
DB_PASSWORD=T3gOIKfniNDE6nPmpE3XONGz
DB_NAME=vigorous_grothendieck
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
```

اگر فایل `.env` وجود ندارد، آن را ایجاد کنید.

## 🚀 مرحله 3: نصب Dependencies

```bash
# از پوشه root پروژه
npm run install:all
```

یا جداگانه:

```bash
# نصب dependencies ریشه
npm install

# نصب frontend dependencies
cd frontend
npm install

# نصب backend dependencies
cd ../backend
npm install
```

## ✅ مرحله 4: تست اتصال به دیتابیس

```bash
cd backend
npm run dev
```

اگر خطایی نداد و پیام "Server is running on port 5000" را دیدید، اتصال موفق بوده است.

**اگر خطا گرفتید:**
- بررسی کنید که IP شما در Liara whitelist شده باشد
- بررسی کنید که اطلاعات `.env` درست باشد
- بررسی کنید که دیتابیس در Liara فعال باشد

## 🎯 مرحله 5: راه‌اندازی Frontend

در یک ترمینال جدید:

```bash
cd frontend
npm run dev
```

یا از پوشه root:

```bash
npm run dev:frontend
```

## 🌐 مرحله 6: دسترسی به اپلیکیشن

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:5000

## 🧪 مرحله 7: تست سیستم

### 1. تست Backend API

```bash
# Health check
curl http://localhost:5000/health
```

باید پاسخ زیر را ببینید:
```json
{"status":"ok","message":"HesabDooni API is running"}
```

### 2. تست Frontend

1. به http://localhost:3000 بروید
2. باید به صفحه `/login` redirect شود
3. برای تست، ابتدا باید یک شرکت از پنل ادمین ایجاد کنید

## 👤 مرحله 8: ایجاد اولین شرکت (از پنل ادمین)

### 1. ورود به پنل ادمین

- به http://localhost:3000/admin/login بروید
- Username: `admin`
- Password: `admin`

> ⚠️ **نکته امنیتی:** این فقط برای توسعه است. در production باید authentication واقعی پیاده‌سازی شود.

### 2. ایجاد شرکت جدید

1. روی "افزودن شرکت جدید" کلیک کنید
2. اطلاعات شرکت را وارد کنید:
   - نام شرکت
   - شناسه ملی
   - نوع شرکت
   - نوع فعالیت (اختیاری)
3. روی "ایجاد شرکت" کلیک کنید
4. **مهم:** Username و Password تولید شده را یادداشت کنید

### 3. ورود با شرکت جدید

1. به http://localhost:3000/login بروید
2. با Username (شناسه ملی) و Password تولید شده وارد شوید
3. باید به داشبورد هدایت شوید

## 📝 مرحله 9: تست افزودن سند

1. از داشبورد، "افزودن سند جدید" را انتخاب کنید
2. یک تصویر روکش سند آپلود کنید
3. OCR باید اطلاعات را استخراج کند
4. اطلاعات را تأیید کنید
5. مستندات مرتبط را آپلود کنید

## ⚙️ تنظیمات اضافی در Liara (اختیاری)

### Whitelist IP Address

اگر نمی‌توانید از MySQL CLI به دیتابیس متصل شوید:

1. در پنل Liara، به بخش Database بروید
2. IP Address خود را whitelist کنید
3. می‌توانید IP فعلی خود را از [whatismyip.com](https://whatismyip.com) پیدا کنید

### Backup دیتابیس

در phpMyAdmin:
1. دیتابیس `vigorous_grothendieck` را انتخاب کنید
2. به تب "Export" بروید
3. Format: SQL
4. روی "Go" کلیک کنید

## 🔍 عیب‌یابی

### مشکل: "Can't connect to MySQL server"

**راه‌حل:**
- بررسی کنید که Host در `.env` درست باشد: `hesabdoni`
- بررسی کنید که Port درست باشد: `3306`
- بررسی کنید که IP شما در Liara whitelist شده باشد

### مشکل: "Access denied for user 'root'"

**راه‌حل:**
- رمز عبور را دوباره بررسی کنید
- از phpMyAdmin بررسی کنید که Username درست باشد

### مشکل: "Unknown database 'vigorous_grothendieck'"

**راه‌حل:**
- بررسی کنید که نام دیتابیس در `.env` درست باشد
- در phpMyAdmin بررسی کنید که دیتابیس وجود دارد

### مشکل: Backend اجرا نمی‌شود

**راه‌حل:**
```bash
# بررسی کنید که dependencies نصب شده باشند
cd backend
npm install

# بررسی کنید که فایل .env وجود دارد
ls -la .env

# بررسی لاگ‌ها
npm run dev
```

### مشکل: Frontend به Backend متصل نمی‌شود

**راه‌حل:**
- بررسی کنید که Backend در حال اجرا باشد (پورت 5000)
- بررسی کنید که `NEXT_PUBLIC_API_URL` در `frontend/.env.local` درست باشد:
  ```env
  NEXT_PUBLIC_API_URL=http://localhost:5000/api
  ```

## ✅ چک‌لیست نهایی

- [ ] جداول در phpMyAdmin ایجاد شده‌اند
- [ ] فایل `backend/.env` با اطلاعات Liara تنظیم شده است
- [ ] Dependencies نصب شده‌اند
- [ ] Backend بدون خطا اجرا می‌شود
- [ ] Frontend بدون خطا اجرا می‌شود
- [ ] می‌توانم به پنل ادمین وارد شوم
- [ ] می‌توانم یک شرکت ایجاد کنم
- [ ] می‌توانم با شرکت ایجاد شده وارد شوم
- [ ] می‌توانم یک سند اضافه کنم

## 🎉 آماده است!

اگر همه چیز کار می‌کند، سیستم شما آماده استفاده است!

